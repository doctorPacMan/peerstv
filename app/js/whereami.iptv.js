Object.assign(Whereami.prototype,{
iptvLoader: function() {
/*
	var serviceUserdata = this.service('user_data');
			serviceTvguide = whereami.services.filter(function(s){return s.type=='tv_guide'}),
			serviceIptv = whereami.services.filter(function(s){return s.type=='iptv'});

	var iptv = this._data.services.filter(s=>{return s.type == 'iptv'}),
		data = [];
	iptv.forEach(service=>{
		var apv, cid = service.contractor.contractorId;
		for(var j=0;j<service.apiVersions.length;j++) {
			apv = service.apiVersions[j];
			//if(apv.majorVersion != this.API_VERSION) continue;
			servapi.push({contractor:cid,location:apv.location});
		}
	});
	return servapi;
*/

	console.log('iptvLoader')

}
});
/*
iptv: function() {
},
*/